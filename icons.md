## Conversão de SVG para SI

Os ícones SVG da aplicação são convertidos para o formato `.si` utilizando a CLI disponibilizada pelo pacote `jovial_svg`.

Execute na raiz do projeto:

```bash
dart run jovial_svg:svg_to_si -o assets/icons/si assets/icons/svg/*.svg